<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="showProgress"
            type="android.databinding.ObservableBoolean" />

        <variable
            name="showNoContacts"
            type="android.databinding.ObservableBoolean"/>

    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/base_padding_medium"
        tools:context=".presentation.contacts.confirmed.ContactsFragment">

        <LinearLayout
            android:id="@+id/contacts_bookmarks_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/base_margin_big"
            android:layout_marginEnd="@dimen/base_margin_big"
            android:layout_marginTop="@dimen/base_margin_small"
            android:background="@drawable/button_background_grey_stroke_rectangle"
            android:orientation="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/contacts_contacts"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:layout_weight="1"
                android:paddingTop="@dimen/base_padding_small"
                android:paddingBottom="@dimen/base_padding_small"
                android:paddingStart="@dimen/base_padding_medium"
                android:paddingEnd="@dimen/base_padding_medium"
                android:maxLines="1"
                android:text="@string/title_contacts_to_specialists"
                android:textSize="@dimen/base_text_size_big" />

            <TextView
                android:id="@+id/contacts_contact_requests"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingTop="@dimen/base_padding_small"
                android:gravity="center"
                android:paddingBottom="@dimen/base_padding_small"
                android:paddingStart="@dimen/base_padding_medium"
                android:paddingEnd="@dimen/base_padding_medium"
                android:maxLines="1"
                android:visibility="gone"
                android:text="@string/title_new_customers_title"
                android:textSize="@dimen/base_text_size_big" />

        </LinearLayout>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/contacts_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/base_margin_small"
            android:layout_marginEnd="@dimen/base_margin_small"
            android:layout_marginTop="@dimen/base_margin_small"
            android:scrollbars="vertical"
            android:paddingBottom="@dimen/design_bottom_navigation_height"
            app:layout_constraintTop_toBottomOf="@+id/contacts_bookmarks_layout">

        </android.support.v7.widget.RecyclerView>

        <ProgressBar
            android:id="@+id/contacts_progress_bar"
            android:layout_width="@dimen/profile_image_half"
            android:layout_height="@dimen/profile_image_half"
            android:theme="@style/Widget.AppCompat.ProgressBar"
            android:visibility="@{showProgress ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/contacts_no_contacts"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/base_text_size_big"
            android:textStyle="italic"
            android:visibility="@{showNoContacts ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </android.support.constraint.ConstraintLayout>
</layout>